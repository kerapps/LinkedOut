name: Build & Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read version from manifest
        id: version
        run: |
          VERSION=$(jq -r '.version' manifest.json)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build zip
        run: |
          zip -r linkedout.zip \
            manifest.json \
            background.js \
            config.js \
            translator.js \
            content.js \
            content.css \
            popup.html \
            popup.js \
            popup.css \
            icons/icon16.png \
            icons/icon48.png \
            icons/icon128.png

      - name: Delete existing release (if any)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release delete "${{ steps.version.outputs.tag }}" --yes --cleanup-tag 2>/dev/null || true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            linkedout.zip \
            --title "LinkedOut ${{ steps.version.outputs.version }}" \
            --notes "Install: download \`linkedout.zip\`, unzip it into a folder, then load unpacked in \`chrome://extensions/\` with Developer mode enabled." \
            --latest
